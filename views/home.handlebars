<h1>Lista de Post: </h1>

{{#each posts}}
  <h1>{{dataValues.titulo}}</h1>
  <p>{{dataValues.conteudo}}</p>
  <a href="/deletar/{{dataValues.id}}"> <button name="deletar">Deletar</button> </a>
  <hr>
{{/each}}
